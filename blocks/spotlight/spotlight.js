export default async function decorate(block) {
  // The block has rows: header, 4 card rows, and a footer CTA row.
  // Structure is already semantic from the plain.html.
  // No additional DOM manipulation needed — CSS handles the layout.
}
