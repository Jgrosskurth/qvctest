export default async function decorate(block) {
  // Each row is a product card with 2 cells: image, text
  // The structure from .plain.html is already correct for our CSS grid
  // No DOM restructuring needed
}
